import React from "react";
import styles from "./footer.module.css";

const Footer = (props) => (
  <footer className={styles.footer}>
    <p className={styles.title}>Rocky Choi</p>
  </footer>
);

export default Footer;
